// @ts-nocheck
/* eslint-disable react/no-array-index-key */
import React, { useEffect } from 'react';

import { TextInput, Select, Form, FormControl, Accordion, SectionHeading, Subheading, Stack, Box, Textarea, Flex } from "@contentful/f36-components";

export const ProgramContents = () => {
  
  return (
    <Box>
      <Subheading>Activities</Subheading>
      This could be where activities are added to a program in order Could
      potentially import Activity Cards from Journey Experience to visualize?
    </Box>
  );
};
